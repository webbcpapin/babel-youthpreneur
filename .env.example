# Google OAuth Client ID is public browser configuration, not a client secret.
# Prefer public/monitoring/config.js for the deployed static website.
VITE_GOOGLE_OAUTH_CLIENT_ID=your-google-oauth-client-id.apps.googleusercontent.com
